This project it's a survey with 6 topics about python and 5 possible answers to vote which one you think it's the correct.

Project created with Django 4.2
To run this app you need follow this steps on the terminal:
    - pip install django
    - go to the project source folder where it's "manage.py"
    - run the following comand: python3 manage.py runserver
    - go to your browser and type 127.0.0.1:8000/polls/